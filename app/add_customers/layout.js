

export const metadata = {
  title: 'Add Customers',
  description: 'Form for adding customers',
}

export default function AddCustomersLayout({ children }) {
  return (
      <main className="grid min-h-screen">{children}</main>
  )
}
